{{- if and .Values.mcp.enabled .Values.mcp.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "openops.serviceAccountName" (dict "root" . "component" "mcp") }}
  labels:
    {{- include "openops.componentLabels" (dict "root" . "component" "mcp") | nindent 4 }}
    {{- with .Values.global.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.mcp.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
